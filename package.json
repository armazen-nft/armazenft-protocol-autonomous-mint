{
  "name": "armazenft-protocol-autonomous-mint",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "build": "npm --prefix frontend install && npm --prefix frontend run build",
    "postinstall": "npm --prefix frontend install || true"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
